(function(exports) {
  function testNoteQuoteToExist() {
    var note = new Note();
    note1 = "Hello";
    inputNote(note1);
    saveNote();
    if (this.noteLog.includes()) {
       return("Working!");
        }
     }
  testNoteQuoteToExist();
  })(this);
